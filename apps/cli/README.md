# lfm CLI

Go daemon: Apple Music → Last.fm scrobbler + optional Discord Rich Presence.

See the [root README](../../README.md) for full docs.

```bash
go build -o lfm .
go test ./...
./lfm version
```
